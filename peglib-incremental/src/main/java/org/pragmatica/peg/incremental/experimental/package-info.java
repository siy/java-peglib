/**
 * Sandbox for the peglib 0.5.0 architectural rework.
 *
 * <p><strong>This package is experimental and additive.</strong> Code here is
 * Phase 0 spike work for the v0.5.0 incremental-native architecture (see
 * {@code docs/incremental/ARCHITECTURE-0.5.0.md}). Nothing in this package is
 * referenced from {@code peglib-core} or the production
 * {@code org.pragmatica.peg.incremental} / {@code .internal} packages — the
 * spike must not perturb the existing 100 incremental tests or the wider 897
 * test corpus.
 *
 * <p>If the Phase 0 GO/NO-GO gate ships GO, types here will be promoted to
 * the production packages during Phases 1–5. If NO-GO, the package is
 * removed wholesale.
 *
 * @since 0.5.0
 */
package org.pragmatica.peg.incremental.experimental;
